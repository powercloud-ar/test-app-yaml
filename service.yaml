apiVersion: v1
kind: Service
metadata:
  name: tiendapc-svc
  labels:
    app.kubernetes.io/app: mitienda
spec:
  selector:
    app: mitienda
  ports:
    - port: 80
      targetPort: 3000
      name: frontend
  type: ClusterIP
